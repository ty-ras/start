export default () => {
  // Modify to e.g. accept Authorization folder value as string parameter, treat it as JWT token, and extract username or other user ID from the token.
  return Promise.resolve(undefined);
};
